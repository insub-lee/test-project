import { createSelector } from 'reselect';

const selectWorkBuilderDetailViewDesigner = state => state.get('work-builder-detail-view-designer');

// Todo - select view list

// Todo - select selected view info

export {};
