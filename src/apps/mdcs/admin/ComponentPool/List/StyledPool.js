import styled from 'styled-components';

const StyledPool = styled.div`
  padding: 3%;
  background-color: white;
  .list_top {
    text-align: right;
    margin-bottom: 10px;
  }
`;

export default StyledPool;
