import styled from 'styled-components';

const Styled = styled.div`
  // width: 200px;
  .style-editor-input {
    width: 300px;
  }

  .blank-divider {
    padding: 10px;
  }
`;

export default Styled;
