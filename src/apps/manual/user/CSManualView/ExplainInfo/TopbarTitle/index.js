import React from 'react';
import Styled from './Styled';

const TopbarTitle = () => (
  <Styled>
    메모
    <span className="topbar-title--num">(12)</span>
  </Styled>
);

export default TopbarTitle;
