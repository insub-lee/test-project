const rowStyle = {
  width: '100%',
  display: 'flex',
  flexFlow: 'row wrap',
};
const colStyle = {
  maxWidth: 1188,
  marginBottom: 16,
  padding: 0,
};
const gutter = 16;

const gridModeStyle = {
  display: 'flex',
  flexFlow: 'column',
};

const basicStyle = {
  rowStyle,
  colStyle,
  gutter,
  gridModeStyle,
};

export default basicStyle;
