import { defineMessages } from 'react-intl';

export default defineMessages({
  changeDispSize: {
    id: 'store.components.PreviewWidget.changeDispSize',
    defaultMessage: 'change dispSize',
  },
});
