const title = '-Input API';
const sub = '제공되는 Input컴포넌트의 API에 대한 설명입니다.';
const list = [
  ['type', '(primary, ghost, dashed, danger, default) 로 설정 가능하며 미설정시 default 값으로 설정', 'string', 'default'],
  ['size', '(small, large, default) 로 설정 가능합니다.', 'string', 'default'],
  ['onClick', 'click 이벤트의 handler를 설정합니다.', '(event) => void', '-'],
];

export { title, sub, list };
