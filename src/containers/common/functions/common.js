// import { fromJS } from 'immutable';
// import _ from 'lodash';

const getPreUrl = (path, str) => path.substr(0, path.indexOf(str) + str.length);

const test = () => {};

export {
  getPreUrl,
  test,
};
