import React from 'react';

import Styled from './Styled';
import Container from './Container';
import Content from './Content';

const FormLayer = () => (
  <Styled className="form-layer">
    <Container>
      <Content>Form Layer</Content>
    </Container>
  </Styled>
);

export default FormLayer;
