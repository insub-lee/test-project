import styled from 'styled-components';

const StyleGrid = styled.div`
  & .ant-input[disabled] {
    background: #000;
    color: #fff !important ;
    text-align: center;
  }
`;

export default StyleGrid;
