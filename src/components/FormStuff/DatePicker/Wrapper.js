import styled from 'styled-components';

const DatePickerWrapper = ComponentName => styled(ComponentName)``;

const MonthPickerWrapper = ComponentName => styled(ComponentName)``;

const RangePickerWrapper = ComponentName => styled(ComponentName)``;

const WeekPickerWrapper = ComponentName => styled(ComponentName)``;

export { DatePickerWrapper, MonthPickerWrapper, RangePickerWrapper, WeekPickerWrapper };
